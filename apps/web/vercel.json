{
  "buildCommand": "cd ../.. && npm install && npm run build --workspace=@flowbit/web",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://flowbit-api.vercel.app/api/$1"
    }
  ]
}
